TUTOR CARA NGEPUSH GITHUB TERPISAH

git remote add origin 'url repo'
git add .
git commit -m"pesan"
(buat branch baru) = $ git checkout -b [name_of_your_new_branch]
(cara push di branch baru) = $git push origin '[name_of_your_new_branch]'
